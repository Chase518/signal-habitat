# Signal: Habitat — Claude Code 入口说明

> 这是一个指针文件。真正的项目规则和当前状态维护在本仓库内部的
> `进度规则与记录/` 子文件夹里（已加入 `.gitignore`，不进版本控制，
> 是跨会话同步用的工作流文档，不是项目代码本身）。每次 session 开始，
> 请先读下面两份文件，再开始工作，不要仅凭对话摘要/记忆行事。

## 必读文件

@进度规则与记录/项目总纲.md
@进度规则与记录/进度追踪.md

> 如果上面的引用没有正确解析，请提醒我确认实际路径，不要假设内容。

## 工作流强制要求

1. 每次会话开始，先确认已读取上面两份文件的最新内容
2. 涉及"要不要扩大项目范围"的讨论，先对照 `项目总纲.md` 里的"明确排除范围"一节，这是本项目过去开发中反复出现过的模式，需要主动提醒我
3. 做出任何有实质影响的设计决策，写一条记录到本repo的 `docs/decisions.md`（这个文件本身在repo内部，随代码一起进版本控制）
4. 完成一个开发阶段或本次会话告一段落时，主动更新 `进度规则与记录/进度追踪.md`：完成状态、关键决策/踩坑、下一步待办 —— 不用等我提醒
5. 如果讨论内容和 `项目总纲.md` 里已定的架构/范围有冲突，先跟我确认，不要默认哪一方优先

## 分支约定

所有开发工作默认都在独立分支上进行，不直接在主开发分支（`main`/`dev`）上改动——这是标准做法，不是只有"实验性"或"不确定"的改动才需要，任何任务开工前先切分支。分支命名按改动性质：`feature/*`（新功能）、`fix/*`（修复）、`refactor/*`（重构）、`docs/*`（文档），不确定归类就用 `task/*`。改完之后由我 review diff，确认满意后自己合并到开发分支；不满意可以直接丢弃整个分支，不影响主线。

## 项目一句话定位

温度/湿度与野生动物活动频率的关联分析 demo，求职portfolio用，非生产系统。完整背景见 `项目总纲.md`。
